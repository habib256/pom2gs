# Tracked conformance-test media

Disk images in this directory are **tracked by Git** as inputs for the
cycle-accuracy catalog (`../catalog.json`), by explicit maintainer decision —
the repository otherwise ignores every disk image (see `.gitignore`). Each file
must be listed here with its provenance and SHA-256 so a catalog entry can pin
it.

| File | Title / author | Provenance | SHA-256 |
|---|---|---|---|
| `textfunk.po` | TextFunk Viewer — Jason Andersen, 2018 | *(to be recorded when the file is added)* | *(to be recorded)* |
